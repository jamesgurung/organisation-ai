# AGENTS.md

## Instructions

- Make the smallest reasonable code change that fully solves the task.
- Prefer concise implementations over broad refactors.
- Match the existing codebase style, structure, naming, and patterns.
- Do not reformat unrelated code.
- Avoid introducing new dependencies unless they are clearly necessary.
- Write clean, concise, maintainable code.
- Keep edits local to the relevant files unless a wider change is required.
- Avoid nullable annotations as this C# feature is disabled.
- Avoid building the project with `dotnet build` as an instance is already running.
- Do not introduce any test projects.
- Do not access `appsettings.json` under any circumstances.
